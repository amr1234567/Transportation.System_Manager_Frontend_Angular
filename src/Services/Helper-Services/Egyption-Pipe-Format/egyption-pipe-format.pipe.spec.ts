import { EgyptionPipeFormatPipe } from './egyption-pipe-format.pipe';

describe('EgyptionPipeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EgyptionPipeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
